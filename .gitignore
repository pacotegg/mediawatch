node_modules/
web/dist/

# Contiene la clave de API de TMDb, la base de datos, las carátulas
# descargadas y las cachés de miniaturas: nada de esto debe compartirse.
data/

# Artefactos de compilación de los clientes
tv/dist/
tv/*.wgt
android/app/build/
android/.gradle/
android/local.properties
*.apk
__pycache__/

# Permisos que Claude Code ha ido aprobando en ESTA maquina. No es un secreto
# -solo lleva comandos y la IP de la LAN-, pero es local por definicion: lo que
# se aprueba aqui no tiene por que valer en otro equipo.
.claude/settings.local.json
